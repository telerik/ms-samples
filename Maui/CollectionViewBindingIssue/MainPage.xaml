<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CollectionViewBindingIssue.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" 
               Text="Collection View with Clear Buttons" 
               FontSize="18" 
               FontAttributes="Bold"
               HorizontalOptions="Center" 
               Margin="20" />

        <Button Grid.Row="1"
                Text="Add Item"
                BackgroundColor="Green"
                TextColor="White"
                HorizontalOptions="Center"
                Margin="20,0,20,20"
                Clicked="OnAddItemClicked" />

        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding Items}"
                        Margin="20">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="10" BackgroundColor="LightGray" Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0" 
                               Text="{Binding Text}" 
                               VerticalOptions="Center" 
                               FontSize="16" />

                        <Button Grid.Column="1" 
                                Text="Clear" 
                                BackgroundColor="Red" 
                                TextColor="White"
                                WidthRequest="80"
                                HeightRequest="40"
                                Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=ClearItemCommand}"
                                CommandParameter="{Binding}" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>
